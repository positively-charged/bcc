#include "phase.h"
#include "semantic/phase.h"

void p_load_library( struct parse* parent ) {
   //struct parse parse;
   //p_init( &parse, parent->task );
   //p_read( &parse );
   //struct semantic semantic;
   //s_init( &semantic, parent->task );
   //s_test( &semantic );
}